<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Proyecto No Iniciado - Dardito</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-gray-100 min-h-screen flex items-center justify-center">
    <div class="container mx-auto px-4">
        <div class="max-w-md mx-auto">
            <!-- Caja principal medio fea pero clara -->
            <div class="bg-gray-800 rounded-lg p-8 border-2 border-gray-600 shadow-2xl text-center">
                <!-- Emoji grande medio ridículo -->
                <div class="text-6xl mb-4">⏳</div>
                
                <h1 class="text-2xl font-bold text-yellow-400 mb-4">
                    Proyecto No Iniciado
                </h1>
                
                <!-- Mensaje principal -->
                <div class="bg-orange-900 border border-orange-700 rounded-lg p-4 mb-6">
                    <p class="text-lg font-semibold text-orange-200">
                        <span class="bg-orange-700 px-2 py-1 rounded font-bold"><%= equipo %></span>
                        <br><br>
                        Todavía no ha iniciado sus proyectos
                    </p>
                </div>

                <!-- Info del usuario -->
                <div class="bg-blue-900 border border-blue-700 rounded-lg p-3 mb-6">
                    <p class="text-blue-200">
                        <span class="font-semibold"><%= user.nickname %></span>
                        <br>
                        <span class="bg-blue-700 px-2 py-1 rounded text-sm"><%= user.rol %></span>
                    </p>
                </div>

                <!-- Mensaje explicativo medio pelo -->
                <div class="bg-gray-700 p-4 rounded border-l-4 border-yellow-500 mb-6">
                    <p class="text-sm text-gray-300">
                        Debes esperar a que el <strong class="text-yellow-400">líder del equipo</strong> 
                        inicialice los proyectos para poder acceder al dashboard.
                    </p>
                </div>

                <!-- Botones medio feos -->
                <div class="space-y-3">
                    
                    
                    <button onclick="window.location.href='/logout'" class="w-full bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-4 rounded border border-red-500 transition-colors">
                        Cerrar Sesión
                    </button>
                </div>

                <!-- Footer medio pelo -->
                <div class="mt-6 pt-4 border-t border-gray-600">
                    <p class="text-xs text-gray-400">
                        Sistema Dardito v1.0 | <%= new Date().getFullYear() %>
                    </p>
                </div>
            </div>

            <!-- Animación medio berreta -->
            <div class="text-center mt-4">
                <div class="inline-flex space-x-1">
                    <div class="w-2 h-2 bg-yellow-500 rounded-full animate-pulse"></div>
                    <div class="w-2 h-2 bg-yellow-500 rounded-full animate-pulse" style="animation-delay: 0.2s;"></div>
                    <div class="w-2 h-2 bg-yellow-500 rounded-full animate-pulse" style="animation-delay: 0.4s;"></div>
                </div>
                <p class="text-xs text-gray-500 mt-2">Esperando inicialización...</p>
            </div>
        </div>
    </div>

    <!-- Script para auto-refresh cada 30 segundos (medio molesto pero funcional) -->
    <script>
        let countdown = 30;
        
        function updateCountdown() {
            const countdownEl = document.getElementById('countdown');
            if (countdownEl) {
                countdownEl.textContent = countdown;
            }
            
            if (countdown <= 0) {
                window.location.reload();
            } else {
                countdown--;
            }
        }
        
        // Auto refresh cada 30 segundos
        setTimeout(() => {
            window.location.reload();
        }, 30000);
        
        // Mostrar countdown (opcional, medio molesto)
        /*
        setInterval(updateCountdown, 1000);
        document.addEventListener('DOMContentLoaded', function() {
            document.querySelector('.container').insertAdjacentHTML('beforeend', 
                '<div class="text-center mt-4"><p class="text-xs text-gray-500">Auto-refresh en: <span id="countdown">30</span>s</p></div>'
            );
        });
        */
    </script>
</body>
</html>