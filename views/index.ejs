<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Dardito</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background-color: #f5f5f5;
            color: #333;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        
        header {
            background-color: #2c3e50;
            color: white;
            padding: 15px 0;
            margin-bottom: 20px;
        }
        
        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        .user-info {
            text-align: right;
        }
        
        .dashboard-container {
            display: grid;
            grid-template-columns: 250px 1fr;
            gap: 20px;
        }
        
        .sidebar {
            background-color: #34495e;
            color: white;
            padding: 20px;
            border-radius: 5px;
            height: fit-content;
        }
        
        .sidebar h3 {
            margin-bottom: 15px;
            border-bottom: 1px solid #4a6278;
            padding-bottom: 10px;
        }
        
        .sidebar ul {
            list-style: none;
        }
        
        .sidebar li {
            margin-bottom: 10px;
        }
        
        .sidebar a {
            color: #ecf0f1;
            text-decoration: none;
            display: block;
            padding: 8px;
            border-radius: 3px;
            transition: background-color 0.2s;
        }
        
        .sidebar a:hover {
            background-color: #4a6278;
        }
        
        .main-content {
            background-color: white;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        
        .card {
            background-color: #f8f9fa;
            border-radius: 5px;
            padding: 15px;
            margin-bottom: 15px;
            border-left: 4px solid #3498db;
        }
        
        .card h3 {
            color: #2c3e50;
            margin-bottom: 10px;
        }
        
        .tarea {
            padding: 10px;
            margin: 8px 0;
            background-color: #fff;
            border-radius: 4px;
            border-left: 4px solid #f39c12;
        }
        
        .tarea.asignada {
            background-color: #fff3cd;
            border-left: 4px solid #ffc107;
        }
        
        .btn {
            padding: 8px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            margin-right: 5px;
            font-weight: 500;
        }
        
        .btn-primary {
            background-color: #3498db;
            color: white;
        }
        
        .btn-secondary {
            background-color: #95a5a6;
            color: white;
        }
        
        .btn-success {
            background-color: #2ecc71;
            color: white;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 15px 0;
        }
        
        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        
        th {
            background-color: #f2f2f2;
        }
        
        .admin-panel {
            background-color: #e74c3c;
            color: white;
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        
        .estadisticas {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 15px;
            margin: 20px 0;
        }
        
        .estadistica-card {
            background-color: #fff;
            padding: 15px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            text-align: center;
        }
        
        .estadistica-card h4 {
            color: #7f8c8d;
            margin-bottom: 10px;
        }
        
        .estadistica-card .valor {
            font-size: 24px;
            font-weight: bold;
            color: #2c3e50;
        }
        
        .proyecto-card {
            background-color: #fff;
            padding: 15px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            margin-bottom: 15px;
            border-left: 4px solid #9b59b6;
        }
        
        footer {
            text-align: center;
            margin-top: 30px;
            padding: 20px;
            color: #7f8c8d;
            border-top: 1px solid #ddd;
        }
    </style>
</head>
<body>
    <header>
        <div class="header-content">
            <h1>Dashboard Dardito</h1>
            <div class="user-info">
                <p>Bienvenido, <strong><%= user.nombre %></strong></p>
                <p>Rol: <strong><%= user.rol %></strong> | Equipo: <strong><%= user.equipo %></strong></p>
            </div>
        </div>
    </header>

    <div class="container">
        <div class="dashboard-container">
            <!-- Sidebar -->
            <div class="sidebar">
                <h3>Navegación</h3>
                <ul>
                    <li><a href="#proyectos">Proyectos</a></li>
                    <li><a href="#tareas">Tareas</a></li>
                    <li><a href="#sprints">Sprints</a></li>
                    <% if (user.rol === 'scrumMaster' || user.rol === 'lider' || user.rol === 'admin') { %>
                        <li><a href="#estadisticas">Estadísticas</a></li>
                    <% } %>
                    <% if (user.rol === 'lider' || user.rol === 'admin') { %>
                        <li><a href="#gestion">Gestión</a></li>
                    <% } %>
                    <% if (user.rol === 'admin') { %>
                        <li><a href="#admin">Panel Admin</a></li>
                    <% } %>
                </ul>
                
                <h3>Proyectos</h3>
                <ul>
                    <li><a href="#proyecto1">Generación T</a></li>
                    <li><a href="#proyecto2">Proyecto</a></li>
                    <% if (user.equipo === 'Grupo1' || user.rol === 'admin') { %>
                        <li><a href="#proyecto3">Proyecto 2</a></li>
                    <% } %>
                </ul>
            </div>

            <!-- Contenido Principal -->
            <div class="main-content">
                <% if (user.rol === 'miembro') { %>
                    <%- include('../dashboard/miembro', {user: user, proyectos: proyectos, tareas: tareas}) %>
                <% } else if (user.rol === 'scrumMaster') { %>
                    <%- include('../dashboard/scrumMaster', {user: user, proyectos: proyectos, tareas: tareas, estadisticas: estadisticas}) %>
                <% } else if (user.rol === 'lider') { %>
                    <%- include('../dashboard/lider', {user: user, proyectos: proyectos, tareas: tareas, estadisticas: estadisticas, equipo: equipo}) %>
                <% } else if (user.rol === 'admin') { %>
                    <%- include('../dashboard/admin', {user: user, proyectos: proyectos, tareas: tareas, estadisticas: estadisticas, equipos: equipos, usuarios: usuarios}) %>
                <% } %>
            </div>
        </div>
    </div>

    <footer>
        <p>Dardito Dashboard &copy; 2023 - Sistema de Gestión Ágil</p>
    </footer>
</body>
</html>