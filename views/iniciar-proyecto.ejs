<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Iniciar Proyecto - Dardito</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-gray-100 min-h-screen">
    <div class="container mx-auto px-4 py-8">
        <!-- Header medio feo pero funcional -->
        <div class="bg-blue-800 rounded-lg p-4 mb-6 border-2 border-blue-600">
            <h1 class="text-2xl font-bold text-center">Inicializar Proyecto</h1>
            <p class="text-center mt-2 text-blue-200">
                Hola <span class="font-semibold"><%= user.nickname %></span> - 
                Equipo: <span class="bg-blue-700 px-2 py-1 rounded"><%= equipo %></span>
            </p>
        </div>

        <div class="max-w-2xl mx-auto">
            <!-- Formulario con estilo medio pelo -->
            <div class="bg-gray-800 rounded-lg p-6 border border-gray-700 shadow-lg">
                <h2 class="text-xl font-semibold mb-4 text-yellow-400">Configuración del Proyecto</h2>
                
                <form action="/iniciar-proyecto" method="POST" class="space-y-4">
                    <!-- Cantidad de proyectos -->
                    <div class="bg-gray-700 p-4 rounded border-l-4 border-green-500">
                        <label class="block text-sm font-medium mb-2">Cantidad de Proyectos</label>
                        <select name="cantidadProyectos" id="cantidadProyectos" class="w-full p-2 bg-gray-800 border border-gray-600 rounded text-white" required>
                            <option value="">-- Selecciona --</option>
                            <option value="2">2 Proyectos (GenT + Proyecto)</option>
                            <option value="3">3 Proyectos (GenT + Proyecto + Proyecto2)</option>
                        </select>
                    </div>

                    <!-- Duración Sprint GenT -->
                    <div class="bg-gray-700 p-4 rounded border-l-4 border-purple-500">
                        <label class="block text-sm font-medium mb-2">Duración Sprint Generación T (semanas)</label>
                        <select name="duracionSprintGenT" class="w-full p-2 bg-gray-800 border border-gray-600 rounded text-white" required>
                            <option value="">-- Selecciona --</option>
                            <option value="1">1 semana</option>
                            <option value="2" selected>2 semanas</option>
                            <option value="3">3 semanas</option>
                            <option value="4">4 semanas</option>
                        </select>
                    </div>

                    <!-- Duración Sprint Proyecto -->
                    <div class="bg-gray-700 p-4 rounded border-l-4 border-red-500">
                        <label class="block text-sm font-medium mb-2">Duración Sprint Proyecto (semanas)</label>
                        <select name="duracionSprintProy" class="w-full p-2 bg-gray-800 border border-gray-600 rounded text-white" required>
                            <option value="">-- Selecciona --</option>
                            <option value="1">1 semana</option>
                            <option value="2" selected>2 semanas</option>
                            <option value="3">3 semanas</option>
                            <option value="4">4 semanas</option>
                        </select>
                    </div>

                    <!-- Duración Sprint Proyecto 2 (oculto por defecto) -->
                    <div id="proyecto2Config" class="bg-gray-700 p-4 rounded border-l-4 border-orange-500 hidden">
                        <label class="block text-sm font-medium mb-2">📋 Duración Sprint Proyecto 2 (semanas)</label>
                        <select name="duracionSprintProy2" class="w-full p-2 bg-gray-800 border border-gray-600 rounded text-white">
                            <option value="">-- Selecciona --</option>
                            <option value="1">1 semana</option>
                            <option value="2" selected>2 semanas</option>
                            <option value="3">3 semanas</option>
                            <option value="4">4 semanas</option>
                        </select>
                    </div>

                    <!-- Botones medio feos pero funcionales -->
                    <div class="flex gap-4 pt-4">
                        <button type="submit" class="flex-1 bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-4 rounded border-2 border-green-500 transition-colors">
                            Inicializar Proyecto
                        </button>
                        <button type="button" onclick="window.location.href='/logout'" class="bg-red-600 hover:bg-red-700 text-white font-bold py-3 px-4 rounded border-2 border-red-500 transition-colors">
                            Salir
                        </button>
                    </div>
                </form>
            </div>

            <!-- Info adicional medio pelo -->
            <div class="mt-6 bg-yellow-900 border border-yellow-700 rounded-lg p-4">
                <h3 class="font-semibold text-yellow-300 mb-2">ℹ️ INFORMACIÓN IMPORTANTE</h3>
                <ul class="text-sm text-yellow-200 space-y-1">
                    <li>• Los sprints comenzarán automáticamente hoy</li>
                    <li>• Podés cambiar las duraciones más tarde si es necesario</li>
                    <li>• El tercer proyecto es para aquellos que se los asigno Tony</li>
                </ul>
            </div>
        </div>
    </div>

    <script>
        // Mostrar/ocultar configuración del tercer proyecto
        document.getElementById('cantidadProyectos').addEventListener('change', function() {
            const proyecto2Config = document.getElementById('proyecto2Config');
            const duracionSprintProy2 = document.querySelector('[name="duracionSprintProy2"]');
            
            if (this.value === '3') {
                proyecto2Config.classList.remove('hidden');
                duracionSprintProy2.required = true;
            } else {
                proyecto2Config.classList.add('hidden');
                duracionSprintProy2.required = false;
                duracionSprintProy2.value = '';
            }
        });
    </script>
</body>
</html>